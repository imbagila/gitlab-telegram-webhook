{
  "name": "gitlab-telegram-webhook",
  "private": true,
  "version": "0.0.0",
  "type": "module",
  "scripts": {
    "dev": "wrangler dev",
    "deploy": "wrangler deploy",
    "build": "tsc --noEmit",
    "types": "wrangler types"
  },
  "dependencies": {
    "@openrouter/sdk": "^0.13.7",
    "zod": "^4.1.12"
  },
  "devDependencies": {
    "typescript": "~6.0.2",
    "wrangler": "^4.103.0"
  }
}
